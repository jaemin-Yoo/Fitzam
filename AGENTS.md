# 저장소 가이드라인

## 에이전트 지침
- 사용자가 다른 언어를 명시적으로 요청하지 않는 한, 모든 사용자 응답은 한국어로 작성한다.
- UTF-8을 명시하여 읽고 쓴다.(예: PowerShell `Get-Content -Encoding UTF8`, `Set-Content -Encoding UTF8`).
- 모든 파일 저장은 UTF-8(BOM 없음)으로 한다.
- 모든 텍스트 파일의 줄바꿈은 CRLF로 유지한다.
